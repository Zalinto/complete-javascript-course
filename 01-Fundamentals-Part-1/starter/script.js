// function getBMI({mass, height}) {
//     return mass / height ** 2;
// }

// const getBMI = ({mass, height}) => {
//     return mass / (height ** 2);
// };

// let [markMass, markHeight] = [78, 1.69];
// let [johnMass, johnHeight] = [92, 1.95];

// const markHigherBMI = getBMI({mass: markMass, height: markHeight}) > getBMI({mass: johnMass, height: johnHeight});

// if (markHigherBMI) {
//     console.log(`Mark's BMI (${getBMI({mass: markMass, height: markHeight})}) is higher than John's!`);
// } else {
//     console.log(`John's BMI (${getBMI({mass: johnMass, height: johnHeight})}) is higher than Mark's!`);
// }
